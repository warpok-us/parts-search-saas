{
  "name": "@partsy/shared-kernel",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm,cjs",
    "dev": "tsup src/index.ts --watch",
    "test": "jest",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@partsy/typescript-config": "workspace:*",
    "tsup": "^8.0.0"
  }
}
